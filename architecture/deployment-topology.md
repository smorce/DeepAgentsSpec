# Deployment Topology

This document captures the runtime deployment assumptions, such as the container
layout, service mesh (if any), ingress points, and observability infrastructure
that support the DeepAgentsSpec microservices.

