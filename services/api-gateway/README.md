# API Gateway

This service handles incoming HTTP traffic, applies routing, and proxies requests to downstream services.

See `service-architecture.md` for more details and `scripts/` for helper commands.

